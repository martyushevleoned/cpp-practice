# Git

## install
```shell
sudo apt install git
git config --global user.name "Martyushev Leoned"
git config --global user.email lyonya.martyushev@mail.ru
git config --global core.ignorecase false
```

## clean
```shell
git clean -ffdx .
git reset --hard HEAD
```